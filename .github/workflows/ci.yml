# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-jars:
    runs-on: ubuntu-latest
    outputs:
      project-version: ${{ steps.bump-version.outputs.version }}
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Extract Maven project version
      run: echo "version=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec)" >> $GITHUB_OUTPUT
      id: bump-version
    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        title: my-art
          - name:"nqueensfaf"
            files:[ target/nqueensfaf-${{ steps.bump-version.outputs.version }}.jar ]
          - name:"nqueensfaf cli"
            files:[ target/nqueensfaf-cli-${{ steps.bump-version.outputs.version }}.jar ]
    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    - name: Update dependency graph
      uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6
    
  build-macos:
    runs-on: macos-latest
    needs: build-jars
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: nqueensfaf-cli-${{ needs.build-jars.outputs.project-version }}-osx
        path: target/nqueensfaf-cli-${{ needs.build-jars.outputs.project-version }}-osx

  build-linux:
    runs-on: ubuntu-latest
    needs: build-jars
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: nqueensfaf-cli-${{ needs.build-jars.outputs.project-version }}-linux
        path: target/nqueensfaf-cli-${{ needs.build-jars.outputs.project-version }}-linux
      
  build-windows:
    runs-on: windows-latest
    needs: build-jars
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: nqueensfaf-cli-${{ needs.build-jars.outputs.project-version }}-windows
        path: target/nqueensfaf-cli-${{ needs.build-jars.outputs.project-version }}-windows
